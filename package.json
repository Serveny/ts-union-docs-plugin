{
    "name": "ts-union-docs-plugin",
    "version": "0.3.3",
    "publisher": "Serveny",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "README.md"
    ],
    "prepublishOnly": "npm run build",
    "repository": {
        "type": "git",
        "url": "https://github.com/Serveny/ts-union-docs-plugin"
    },
    "devDependencies": {
        "@types/node": "26.1.2",
        "@vitest/ui": "^4.0.18",
        "rollup-plugin-node-externals": "9.0.1",
        "typescript": "^6.0.3",
        "vite": "8.1.5",
        "vite-plugin-dts": "5.0.3",
        "vitest": "^4.0.18"
    },
    "scripts": {
        "check": "tsc --noEmit",
        "build": "vite build",
        "watch": "vite",
        "increment-version": "node --experimental-strip-types scripts/increment-version.ts",
        "refresh": "npm run build && cd ../example && npm i",
        "watch-and-refresh": "npx nodemon -w ./src -e ts -x \"npm run build && cd ../example && npm i\"",
        "debug": "TSS_DEBUG=5667 code ../example --user-data-dir ~/.vscode-debug/ --disable-extensions",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:ui": "vitest --ui"
    }
}
